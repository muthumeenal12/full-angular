import { TeamSizeFilterPipe } from './team-size-filter.pipe';

describe('TeamSizeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamSizeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
